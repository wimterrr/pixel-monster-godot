# Save Anchor Contract

The first proof needs a single save receipt with three fields:

- `checkpoint_id`
- `roster_delta`
- `spent_items`

Nothing else is required before the first cold-reload pass.
